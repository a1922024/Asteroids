#ifndef PLAYER_H
#define PLAYER_H
#include"Projectile.h"
/*
* Class for the player. Adds extra attributes/functions onto projectile as needed. You should be able to
* implement the controls entirely within the game loop (main function in main.cpp) using the asteroid 
* array functions, but do whatever works lol.
*/
class player : public projectile{
};
#endif
